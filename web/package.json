{
  "name": "bunnyland-rl-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@bunnyland/ui-web": "file:../../bunnyland-ui-web"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
